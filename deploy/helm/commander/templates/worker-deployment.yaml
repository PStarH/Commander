{{- if not .Values.tenantAuthority.transportBootstrap }}
# =============================================================================
# Commander — Worker Deployment
# =============================================================================
{{- if .Values.worker.enabled }}
{{- include "commander.requireEnterpriseSecrets" . }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "commander.fullname" . }}-worker
  labels:
    {{- include "commander.labels" . | nindent 4 }}
    app.kubernetes.io/component: worker
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      {{- include "commander.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "commander.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets: {{ toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "commander.serviceAccountName" . }}
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: {{ .Values.worker.terminationGracePeriodSeconds }}
      securityContext: {{ toYaml .Values.podSecurityContext | nindent 8 }}
      {{- if include "commander.postgresBackend" . }}
      initContainers:
        {{- include "commander.runtimeMigrationGate" (dict "root" . "key" (include "commander.databaseWorkerSecretKey" .)) | nindent 8 }}
      {{- end }}
      containers:
        - name: worker
          image: "{{ .Values.worker.image.repository | default .Values.image.repository }}:{{ .Values.worker.image.tag | default .Values.image.tag }}"
          imagePullPolicy: {{ .Values.worker.image.pullPolicy | default .Values.image.pullPolicy }}
          ports:
            - name: health
              containerPort: {{ .Values.worker.health.port }}
          env:
            - name: NODE_ENV
              value: {{ .Values.config.nodeEnv | quote }}
            {{- include "commander.profileTierEnv" . | nindent 12 }}
            - name: COMMANDER_KERNEL_ENABLED
              value: "1"
            - name: COMMANDER_WORKER_HEALTH_PORT
              value: {{ .Values.worker.health.port | quote }}
            {{- if include "commander.postgresBackend" . }}
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "commander.databaseUrlSecretName" . | quote }}
                  key: {{ include "commander.databaseWorkerSecretKey" . | quote }}
            - name: COMMANDER_KERNEL_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "commander.databaseUrlSecretName" . | quote }}
                  key: {{ include "commander.databaseWorkerSecretKey" . | quote }}
            - name: COMMANDER_KERNEL_BACKEND
              value: postgres
            {{- include "commander.databaseTlsEnv" . | nindent 12 }}
            {{- end }}
            - name: COMMANDER_WORKER_BOOTSTRAP
              value: {{ .Values.worker.bootstrap | quote }}
            - name: COMMANDER_WORKER_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ include "commander.workerTokenSecretName" . | quote }}
                  key: {{ .Values.worker.authTokenSecretKey | default "token" | quote }}
            - name: COMMANDER_WORKER_AUTH_SUBJECT
              value: {{ .Values.worker.authSubject | quote }}
            - name: COMMANDER_WORKER_KIND
              value: {{ .Values.worker.kind | quote }}
            - name: COMMANDER_WORKER_CAPABILITIES
              value: {{ .Values.worker.capabilities | quote }}
            - name: COMMANDER_WORKER_MAX_CONCURRENCY
              value: {{ .Values.worker.maxConcurrency | quote }}
            - name: COMMANDER_WORKER_TENANTS
              value: {{ .Values.worker.tenants | quote }}
            - name: COMMANDER_CELL_TENANT_ID
              value: {{ include "commander.cellTenantId" . | quote }}
            {{- if .Values.adapterOps.kubernetes.cluster }}
            - name: COMMANDER_KUBERNETES_CLUSTER
              value: {{ .Values.adapterOps.kubernetes.cluster | quote }}
            - name: COMMANDER_KUBERNETES_SERVER
              value: {{ .Values.adapterOps.kubernetes.server | quote }}
            - name: COMMANDER_KUBERNETES_NAMESPACES
              value: {{ join "," .Values.adapterOps.kubernetes.namespaces | quote }}
            - name: COMMANDER_KUBERNETES_TOKEN_ENV
              value: {{ .Values.adapterOps.kubernetes.tokenEnv | quote }}
            - name: {{ .Values.adapterOps.kubernetes.tokenEnv }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.adapterOps.secrets.existingSecret | quote }}
                  key: {{ .Values.adapterOps.kubernetes.tokenSecretKey | quote }}
            {{- end }}
            - name: COMMANDER_WORKER_POLL_MS
              value: {{ .Values.worker.pollMs | quote }}
            - name: COMMANDER_WORKER_LEASE_TTL_MS
              value: {{ .Values.worker.leaseTtlMs | quote }}
            - name: COMMANDER_WORKER_HEARTBEAT_MS
              value: {{ .Values.worker.heartbeatMs | quote }}
            {{- include "commander.capabilityEnv" . | nindent 12 }}
            {{- include "commander.evidenceSigningEnv" . | nindent 12 }}
          resources: {{ toYaml .Values.worker.resources | nindent 12 }}
          securityContext:
            {{- include "commander.cellSecurityContext" . | nindent 12 }}
          livenessProbe:
            httpGet:
              path: {{ .Values.worker.health.livenessPath }}
              port: health
            initialDelaySeconds: {{ .Values.worker.health.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.health.periodSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.worker.health.readinessPath }}
              port: health
            initialDelaySeconds: {{ .Values.worker.health.initialDelaySeconds }}
            periodSeconds: {{ .Values.worker.health.periodSeconds }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            {{- if include "commander.postgresBackend" . }}
            {{- include "commander.databaseTlsVolumeMount" . | nindent 12 }}
            {{- end }}
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if include "commander.postgresBackend" . }}
        {{- include "commander.databaseTlsVolume" . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity: {{ toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{ toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}
