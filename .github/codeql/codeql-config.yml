name: Commander CodeQL

paths-ignore:
  - '**/test/**'
  - '**/tests/**'
  - '**/*.test.ts'
  - '**/*.spec.ts'
  - '**/*.e2e.test.ts'
  - '**/__tests__/**'
  - '**/conformance/**'
  - '**/node_modules/**'
  - '**/dist/**'

# Global Express rateLimitMiddleware covers /v1 routes; CodeQL cannot see
# cross-file middleware, so missing-rate-limiting on auth handlers is noise.
# Helm values force empty inline password (maxLength: 0) — secrets via existingSecret.
query-filters:
  - exclude:
      id: js/missing-rate-limiting
  - exclude:
      id: js/empty-password-in-configuration
      paths:
        - deploy/helm/**
        - helm/**
