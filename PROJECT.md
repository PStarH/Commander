# PROJECT.md - Commander Project Configuration

> This file is loaded at session start and defines the project-level constraints and standards.
> Based on Claude Code's Layer 3 Memory Architecture.

## Project Overview

**Name:** Commander
**Codename:** war-room
**Objective:** A task command center for "small team + multiple AI Agents" - making AI a team member with badge and responsibilities.

## Architecture

### Monorepo Structure

```
Commander/
├── apps/
│   ├── api/          # Node.js + Express backend
│   └── web/          # Vite + React frontend
├── packages/
│   └── core/         # Domain models and logic
├── docs/             # PRD, research notes
└── memory/           # Memory index (Layer 2)
```

### Core Concepts

1. **War Room**: Project state snapshot (Project / Agents / Missions / Logs / BattleReport)
2. **Project Memory**: Distilled experience (DECISION / ISSUE / LESSON / SUMMARY)
3. **Agent State**: Per-agent continuity (summary / preferences / tags)
4. **Governance Mode**: AUTO / GUARDED / MANUAL - controls execution boundaries

## Coding Standards

### TypeScript

- Strict mode enabled
- Prefer `interface` over `type` for object shapes
- Use `const enum` for fixed string unions
- Explicit return types for public functions

### Naming Conventions

- Files: `camelCase.ts` for modules, `PascalCase.tsx` for components
- Interfaces: `PascalCase` (e.g., `ProjectMemoryItem`)
- Functions: `camelCase` (e.g., `calculateGovernanceStats`)
- Constants: `SCREAMING_SNAKE_CASE` for true constants

### API Design

- RESTful: `/projects/:projectId/missions`
- JSON responses, proper HTTP status codes
- CORS enabled for local development

## Testing

```bash
# Run tests (when available)
pnpm test

# Type check
pnpm -r build
```

## Development Commands

```bash
# Start API
pnpm dev:api

# Start Web
pnpm dev:web

# Start both
pnpm dev
```

## Key Files to Know

| File | Purpose |
|------|---------|
| `apps/api/src/index.ts` | Main API routes |
| `apps/api/src/store.ts` | In-memory state management |
| `apps/api/src/memoryStore.ts` | ProjectMemory CRUD |
| `apps/api/src/governanceObserver.ts` | Governance statistics |
| `packages/core/src/index.ts` | Domain types and logic |
| `docs/iterative-research.md` | Research findings |

## Memory System

### Layer 1: In-Context (Active Window)
- Current conversation and tool outputs
- Handled automatically by LLM

### Layer 2: Memory Index (pointer system)
- `memory/index.json` - Pointer to domain files
- Domains: Project Context, Decisions, Patterns, Preferences, Issues, Lessons
- API: `/projects/:projectId/memory-index/domains`

### Layer 3: PROJECT.md (this file)
- Project-level static config
- Loaded at session start
- Rarely modified

## Governance Model

### Mission Risk Levels
- `LOW`: Routine tasks, AUTO execution
- `MEDIUM`: Standard tasks, GUARDED (Sentinel monitoring)
- `HIGH`: Critical tasks, MANUAL approval required
- `CRITICAL`: Never auto-execute, human approval mandatory

### Agent Roles
- `COMMANDER`: Strategic decisions
- `SENATE`: Review and approval
- `EXECUTOR`: Task execution

## Known Issues

1. In-memory storage (data lost on restart) - by design for v0
2. No authentication (local development only)
3. No RAG/semantic search for memory (planned)

## Future Improvements

See `docs/iterative-research.md` for research-backed improvements:
- Token Budget mechanism
- Parallel agent execution
- Semantic memory retrieval
- MCP/A2A protocol integration

---

*This file was auto-generated by Commander Implementation Agent*
*Last updated: 2026-04-09*
