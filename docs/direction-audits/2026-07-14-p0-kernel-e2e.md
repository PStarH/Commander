# Direction Audit — P0 kernel e2e closure

**Date:** 2026-07-14  
**Branch:** `p0/v2-kernel-e2e-closure`  
**Workflow:** `wf_99983a5c-7b2` (4 evidence packs + audit + 3-lens verify)  
**Final decision:** **Defer** (refuteCount 0 / 3 Keep)

## Evidence Rule summary

| Claim                                                               | Status                                             | Home                                                              |
| ------------------------------------------------------------------- | -------------------------------------------------- | ----------------------------------------------------------------- |
| PG claim/complete/fail bind `workerGeneration` + live ACTIVE worker | **ENFORCED**                                       | `packages/kernel/src/postgres.ts` + integration tests + CI        |
| Reclaim + zombie complete rejected (no dual success)                | **ENFORCED**                                       | `packages/worker-plane/src/e2e/gateway-kernel-worker.e2e.test.ts` |
| Gateway dist → worker → SUCCEEDED                                   | **ENFORCED**                                       | `pnpm p0:full-loop` + `.github/workflows/p0-kernel-e2e.yml`       |
| Dedicated P0 Kernel E2E workflow green                              | **ENFORCED**                                       | Actions runs e.g. 29338185230 / 29338183221                       |
| `scripts/p0-kernel-e2e.ts` proves fencing/terminal                  | **MISSING** (by design — soft probe; header stale) | scaffold only                                                     |
| Process-level SIGKILL of live WorkerService                         | **PARTIAL**                                        | e2e uses lease-TTL + reclaim API, not SIGKILL                     |
| Kernel default-on / zero-flag durable runs                          | **VIOLATED**                                       | still `COMMANDER_KERNEL_ENABLED` opt-in outside prod refuse       |
| Live worker effect admission fail-closed                            | **PARTIAL**                                        | broker EXISTS; bootstrap `permit-default` allow-all               |
| Monorepo Quality Gates green on branch                              | **MISSING**                                        | kernel jobs green; overall CI red (run 29338183655)               |

## Decision

**Defer** further “P0 kernel e2e closure” implementation.

P0 north star under `COMMANDER_KERNEL_ENABLED=1` + Postgres is already proven by package e2e + full-loop + dedicated CI. More harnesses expand verification surface without closing product release blockers. Duplication delta for more P0 work: **grow or flat, not shrink**.

## Definition of Done (P0 closeout hygiene only)

1. Keep existing CI surface as merge gate for the kernel substrate (do not add new P0-only packages).
2. Optional: fix stale claims in `scripts/p0-kernel-e2e.ts` header + `docker-compose.v2.yml` “C1/C2 pending”.
3. Treat monorepo Quality Gates green as **separate** merge DoD (not more fencing).

## Ranked next candidates (release value)

1. **Single durable run authority** — kernel default path for `/v1` runs; demote WarRoomStore/RunLedger as authorities (`v1GatewayKernel.ts`, `store.ts`, PRINCIPLES §2.3/§4).
2. **Effect-broker admission force** on live worker bootstrap — replace permit-default (`packages/worker-plane/src/bootstrap.ts`).
3. **Monorepo CI green** + claim/doc honesty (merge unblock).
4. Dual memory/store consolidation + count-guard.
5. Publish-claim honesty (README npm-pending, false durability labels).

## Release implication

“v2 can publish” is **not** blocked by missing fencing proofs. It is blocked by dual run authorities, allow-all effect policy on live worker, monorepo CI, and overstated status/marketing. WP7 freeze still applies.

## Provenance

- Evidence + adversarial verify: workflow `wf_99983a5c-7b2`
- PRINCIPLES restored to repo root from `.internal/docs/status/PRINCIPLES.md` (was gitignored-only)
- Status page: `docs/p0-parallel-status.md`
