chore: format-sweep (auto-fix 65+ latent Prettier violations)

The pre-push protocol landed in 5b04d6a6 enforces a Prettier-clean
baseline across the development surface. The current working tree has
accumulated 65+ latently-drifted files (output of
`pnpm exec prettier --check packages/*/src packages/*/tests apps/*/src scripts`).

This commit auto-fixes them via `pnpm format` so:
  - remote CI's Prettier format check step can pass on the next push,
  - the new pre-push hook lands clean rather than blocking every push,
  - developers' next `git push` succeeds (modulo better-sqlite3 install).
No semantic change: pure whitespace + minor formatting moves under
.prettierrc + `5b04d6a6`'s broader tsconfig settings.
