/**
 * REMOVED 2026-07-15 — LockFreeStateStore was an orphan (zero importers).
 *
 * This stub intentionally exports no class so the §3 store count-guard does not
 * re-count a dead implementation. Do not reintroduce LockFreeStateStore without
 * a Direction Audit that names a single consumer and a test.
 *
 * Historical: CAS in-memory store implementing a local ILockFreeStateStore.
 */
export {};
