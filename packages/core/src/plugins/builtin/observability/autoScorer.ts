/**
 * Re-export of canonical `observability/autoScorer.ts` (was a verbatim duplicate).
 * Collapsed 2026-07-15 for PRINCIPLES §3 / DRY.
 */
export {
  AutoScorer,
  type AutoScorerConfig,
  type AutoScoreResult,
} from '../../../observability/autoScorer';
