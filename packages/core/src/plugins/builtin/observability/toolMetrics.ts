/**
 * Re-export of canonical `observability/toolMetrics.ts` (was a verbatim duplicate).
 * Collapsed 2026-07-15 for PRINCIPLES §3 / DRY.
 */
export {
  ToolMetricsCollector,
  getToolMetricsCollector,
  resetToolMetricsCollector,
} from '../../../observability/toolMetrics';
