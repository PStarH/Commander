# Supply-chain hardening: do not run lifecycle scripts on install.
# Post-install scripts are a common RCE vector for compromised dependencies.
# Native modules must be rebuilt explicitly with `pnpm rebuild`.
ignore-scripts=true

# Ensure lockfile is respected and not silently updated.
prefer-frozen-lockfile=true
